import React from "react";
import ReactDOM from "react-dom/client";
import CipherStudioApp from "./CipherStudioApp";

const root = ReactDOM.createRoot(document.getElementById("root"));
root.render(<CipherStudioApp />);
